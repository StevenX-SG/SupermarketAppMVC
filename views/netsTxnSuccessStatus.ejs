<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful - NETS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .success-container {
            max-width: 500px;
            background: white;
            border-radius: 10px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .checkmark {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            border-radius: 50%;
            background: #28a745;
            display: flex;
            align-items: center;
            justify-content: center;
            animation: scaleIn 0.5s ease-in;
        }
        .checkmark svg {
            width: 50px;
            height: 50px;
            color: white;
            stroke-width: 2;
        }
        @keyframes scaleIn {
            0% { transform: scale(0); }
            100% { transform: scale(1); }
        }
        h2 {
            color: #333;
            margin: 20px 0;
        }
        .message {
            color: #666;
            margin-bottom: 30px;
            font-size: 16px;
        }
        .timer {
            font-size: 14px;
            color: #999;
            margin-bottom: 20px;
        }
        .timer strong {
            color: #667eea;
            font-size: 18px;
        }
        .btn-orders {
            background-color: #667eea;
            border: none;
            color: white;
            padding: 10px 30px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: 600;
            transition: background-color 0.3s ease;
            display: inline-block;
        }
        .btn-orders:hover {
            background-color: #764ba2;
            color: white;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="checkmark">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"></path>
            </svg>
        </div>
        
        <h2>Payment Successful!</h2>
        
        <p class="message">
            Your NETS QR payment has been processed successfully.<br>
            Your order is being created...
        </p>
        
        <div class="timer">
            Redirecting to orders in: <strong id="countdown">5</strong> seconds
        </div>
        
        <% if (orderId) { %>
            <a href="/orders/<%= orderId %>" class="btn-orders">View Order Now</a>
        <% } else { %>
            <a href="/orders" class="btn-orders">Go to Orders Now</a>
        <% } %>
    </div>

    <script>
        let secondsLeft = 5;
        const countdownElement = document.getElementById('countdown');
        const orderId = '<%= orderId %>';
        
        const timer = setInterval(() => {
            secondsLeft--;
            countdownElement.textContent = secondsLeft;
            
            if (secondsLeft <= 0) {
                clearInterval(timer);
                if (orderId) {
                    window.location.href = '/orders/' + orderId;
                } else {
                    window.location.href = '/orders';
                }
            }
        }, 1000);
    </script>
</body>
</html>
