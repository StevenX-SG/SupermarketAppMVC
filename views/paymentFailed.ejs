<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Failed - Supermarket</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/custom.css">
  <style>
    .error-container {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 60vh;
      padding: 40px 20px;
    }
    .error-card {
      max-width: 600px;
      width: 100%;
      border: none;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      border-radius: 12px;
    }
    .error-header {
      background: linear-gradient(135deg, #dc3545 0%, #e74c3c 100%);
      color: white;
      padding: 40px 20px;
      text-align: center;
      border-radius: 12px 12px 0 0;
    }
    .error-icon {
      font-size: 60px;
      margin-bottom: 15px;
    }
    .error-title {
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 10px;
    }
    .error-subtitle {
      font-size: 16px;
      opacity: 0.9;
      margin: 0;
    }
    .error-details {
      padding: 30px;
    }
    .alert-box {
      background-color: #fff3cd;
      border: 1px solid #ffc107;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 20px;
    }
    .alert-box-error {
      background-color: #f8d7da;
      border: 1px solid #f5c6cb;
      color: #721c24;
    }
    .alert-box-title {
      font-weight: 600;
      margin-bottom: 8px;
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .alert-box-message {
      font-size: 15px;
      line-height: 1.6;
      margin: 0;
      word-break: break-word;
    }
    .reason-box {
      background-color: #f8f9fa;
      border-left: 4px solid #dc3545;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
    .reason-label {
      font-size: 12px;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }
    .reason-text {
      font-size: 15px;
      color: #212529;
      margin: 0;
    }
    .help-text {
      color: #666;
      font-size: 14px;
      margin-bottom: 20px;
      line-height: 1.6;
    }
    .action-buttons {
      padding: 30px;
      background-color: #f8f9fa;
      border-radius: 0 0 12px 12px;
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }
    .action-buttons a,
    .action-buttons button {
      flex: 1;
      min-width: 150px;
    }
    .troubleshooting {
      background-color: #e7f3ff;
      border-left: 4px solid #0066cc;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
    .troubleshooting-title {
      font-weight: 600;
      color: #0066cc;
      margin-bottom: 10px;
      font-size: 14px;
    }
    .troubleshooting-list {
      margin: 0;
      padding-left: 20px;
      font-size: 14px;
      color: #333;
    }
    .troubleshooting-list li {
      margin-bottom: 8px;
      line-height: 1.5;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <div class="error-container">
    <div class="error-card">
      <!-- Error Header -->
      <div class="error-header">
        <div class="error-icon">✕</div>
        <div class="error-title">Payment Failed</div>
        <p class="error-subtitle">Unable to process your payment</p>
      </div>

      <!-- Error Details -->
      <div class="error-details">
        <!-- Error Reason -->
        <% if (reason) { %>
          <div class="reason-box">
            <div class="reason-label">Reason</div>
            <p class="reason-text"><%= reason %></p>
          </div>
        <% } %>

        <!-- Error Message -->
        <% if (errorMessage) { %>
          <div class="alert-box alert-box-error">
            <div class="alert-box-title">Error Details</div>
            <p class="alert-box-message"><%= errorMessage %></p>
          </div>
        <% } %>

        <!-- Help Text -->
        <p class="help-text">
          Your payment was not completed. Please review the details below and try again, 
          or contact our support team if the problem persists.
        </p>

        <!-- Troubleshooting Tips -->
        <div class="troubleshooting">
          <div class="troubleshooting-title">💡 Troubleshooting Tips</div>
          <ul class="troubleshooting-list">
            <li>Check your payment details (card number, expiry date, CVV)</li>
            <li>Ensure sufficient funds in your account</li>
            <li>Verify your internet connection is stable</li>
            <li>Try a different payment method if available</li>
            <li>Contact your bank if you continue experiencing issues</li>
          </ul>
        </div>

        <!-- Transaction Reference (if available) -->
        <% if (transactionId) { %>
          <div class="alert-box">
            <div class="alert-box-title">Transaction Reference</div>
            <p class="alert-box-message" style="font-family: 'Courier New', monospace; word-break: break-all;">
              <%= transactionId %>
            </p>
          </div>
        <% } %>
      </div>

      <!-- Action Buttons -->
      <div class="action-buttons">
        <a href="/checkout" class="btn btn-danger">
          Retry Payment
        </a>
        <a href="/cart" class="btn btn-outline-secondary">
          Back to Cart
        </a>
        <a href="/shopping" class="btn btn-outline-secondary">
          Continue Shopping
        </a>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
